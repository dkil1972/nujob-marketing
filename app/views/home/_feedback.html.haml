%li.feedback-item
  .feedback-content
    = feedback[:content].truncate(length, separator: ' ')
    .feedback-arrow
  .feedback-author
    .feedback-icon
      = image_tag(feedback[:icon])
    .feedback-name.font-24
      = feedback[:user]
    .feedback-role
      = feedback[:role]